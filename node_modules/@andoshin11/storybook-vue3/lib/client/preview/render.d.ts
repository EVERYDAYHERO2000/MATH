import { RenderContext } from './types';
export declare const propsContainer: {
    props: {
        [x: string]: any;
    };
};
export default function render({ storyFn, kind, name, args, showMain, showError, showException }: RenderContext): void;
